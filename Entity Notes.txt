PropertiesObject (abstract)

Map (PropertiesObject)
    Properties
        Sky
        Lighting settings
        Fog

Face (PropertiesObject)
    Properties
        Material : no material allowed for substance faces
        Overlay material or none
        Orientation : rotate/mirror
        Portal target

Entity (abstract PropertiesObject)
    Attributes
        Behaviors
        Sensor
    Properties
        X-ray? : only for editor; makes object transparent allowing you to zoom/select through it
        Tag : limited selection; colors and shapes
        Invert sensor?
        Turn on time
        Max on time, or infinite
        Turn off time, or infinite
        Health

Object (abstract Entity) : objects take up a certain fixed size block of voxels
    Attributes
        Orient face : aligns to center of face
    Properties
        Rotation about face normal

Substance (Entity)
    Attributes
        Voxels
    Properties
        Flat? : substance becomes a flat, double sided plane


Sensor (abstract PropertiesObject)

ActivatedSensor (abstract Sensor)
    Properties
        Activtor entity filter: required base type for entity/behaviors, tag, or specific entity

OffSensor (Sensor)

InputSensor (Sensor)
    Properties
        Input entities
        For each input entity:
            Change on turn on: -1/0/1
            Change on turn off: -1/0/1
        Input threshold

TouchSensor (ActivatedSensor)

InRangeSensor (ActivatedSensor)
    Properties
        Look range

PlayerLookAtSensor (Sensor)

PlayerUseSensor (Sensor)

MovementSensor (Sensor)


Character (abstract Object)

Player (Character) : 1 in every map

Selection of characters

Selection of meshes with skin choices
    Ball
    [others]

Tree (abstract Object) : procedural tree
    Properties
        [tree generation parameters]

Selection of trees


Behavior (abstract Entity)
    Properties
        Condition: On/Off/Both
        Optionally apply behavior to another entity instead, or whichever entities activated the ActivatedSensor

Visible (Behavior)

Solid (Behavior)

Hurt/heal (Behavior)
    Properties
        Amount
        Continuous?

Clone (Behavior) : once when enabled. clone starts at initial position with initial properties

Teleport (Behavior)
    Properties
        1 or 2 target entities : With 2 entities, move the relative distance between them

Physics (Behavior for Entity with Solid)
    Properties
        Can be picked up?
        Velocity
        [physics parameters including breakability]

Propel (Behavior for Entity with Physics)
    Properties
        Direction, speed

PropelTowards (Behavior for Entity with Physics)
    Properties
        Entity
        Speed
        Arc angle

Thruster (Behavior for Entity with Physics)
    Properties
        Direction
        Strength

Sound (Behavior)
    Properties
        Sound
        Loop?
        Volume
        Speed
        Start fade dist
        End fade dist

Speech bubble (Behavior)
    Properties
        Message

MovementBehavior (abstract Behavior) : different objects will achieve movement in different ways. For example, characters will walk, and meshes will float
    Properties
        Max speed

MoveWith (MovementBehavior) : like parenting
    Properties
        Entity

MoveInDirection (MovementBehavior)
    Properties
        Direction

MoveToward (MovementBehavior)
    Properties
        Entity

MoveAway (MovementBehavior)
    Properties
        Entity

Spin (MovementBehavior)

Rotate to (MovementBehavior)
    Properties
        Rotation

FaceToward (MovementBehavior)
    Properties
        Opposite?
        Entity

NavigateToward (MovementBehavior)
    Properties
        Entity

NavigateAway (MovementBehavior)
    Properties
        Entity

Wander (MovementBehavior)
    Properties
        Change direction rate
        Pause frequency (percentage)
        Focus entity
        Range

Fog (Behavior for Substance with Visible) : volumetric fog
    [properties]

Glow (Behavior for Substance with Visible) : a flare effect that emits light
    Properties
        Color
        Intensity

Fire (Behavior for Substance with Visible)

ParticleEffect (abstract Behavior for Substance with Visible)
    Properties
        Color
        Size
        Density

Rain (ParticleEffect)

Snow (ParticleEffect)

Dust (ParticleEffect)

Wind (Behavior for Substance with ParticleEffect) : only affects particles
    Properties
        Direction
        Speed

Fluid (Behavior for Substance)
    Properties
        Resistance
        Buoyancy

Push (Behavior for Substance)
    Properties
        Direction
        Target velocity
        Strength

Climbable (Behavior for Substance with Solid)
